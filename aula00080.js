const mat = document.getElementById("mat");
const num = Math.floor(Math.random()*10) + 1; // Método da biblioteca math que gera um número aleatório 

//mat.innerHTML = Math.round(num); // Método que aredonda para baixo um número
//mat.innerHTML = Math.pow(10,3); // para potencias
mat.innerHTML = num